# Persaille

Static site for Persaille — tech agency and portfolio of Dale Percelay.

Plain HTML / CSS. No build step.

## Local

Open `index.html` in a browser, or serve the directory:

```
python3 -m http.server 8080
```

## Resume

Drop a `resume.pdf` at the repo root to wire up the founder section's "RESUME" link.
